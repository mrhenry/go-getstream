box: simonmenke/go-build:latest

build:
  steps:
  - setup-go-workspace

  - script:
      name: go build
      code: |
        CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build
  - script:
      name: go test
      code: |
        go test
  after-steps:
